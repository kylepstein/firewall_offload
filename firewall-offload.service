[Unit]
Description=Nvidia Firewall Offload Daemon
After=network.target network.service networking.service

[Service]
Type=simple
ExecStart=/usr/sbin/firewall_offload

[Install]
WantedBy=multi-user.target
